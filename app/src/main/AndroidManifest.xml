<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kelompok3.rplinformatika.svhouse">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".loginAndRegister.AwalActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".loginAndRegister.LoginActivity" />
        <activity android:name=".loginAndRegister.RegisterActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".Developer.UploadActivity" />
        <activity android:name=".RumahActivity.RumahActivity" />
        <activity android:name=".RumahActivity.PesanActivity" />
        <activity android:name=".Pemesanan.PemesananActivity" />
        <activity android:name=".Pesanan.PesananUser" />
        <activity android:name=".KeranjangActivity" />
        <activity android:name=".NotifyActivity"></activity>
    </application>

</manifest>